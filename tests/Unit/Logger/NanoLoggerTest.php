<?php

/**
 * NanoLoggerのテストは削除されました。
 * 
 * 理由：
 * - NanoLoggerクラスは`error_log`関数を直接呼び出しており、これをモックすることが困難です。
 * - テストでNanoLoggerを継承してlogメソッドをオーバーライドすると、実際のNanoLoggerの振る舞いをテストしていないことになります。
 * - PHPのグローバル関数をモックする適切な方法がないため、意味のあるテストを書くことが難しいです。
 */
